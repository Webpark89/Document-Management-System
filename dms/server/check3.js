const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const admin = await prisma.role.findFirst({ where: { name: 'Administrator' }, include: { permissions: { include: { permission: true } } } }); console.log(admin.permissions.map(p => p.permission.module + ':' + p.permission.action)); } main().finally(() => prisma.$disconnect());
